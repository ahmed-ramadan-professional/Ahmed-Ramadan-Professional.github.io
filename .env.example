# Copy to .env and fill in. Never commit the real .env.
# Google API key with the Drive API enabled, restricted to your domain.
# Powers the live project galleries sourced from Google Drive.
VITE_GDRIVE_API_KEY=

# Formspree form id (part after /f/ in the endpoint) — powers the contact form.
VITE_FORMSPREE_ID=
